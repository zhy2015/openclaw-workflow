# Legacy Archived Skills (Compatibility Hold Area)

This directory is a legacy compatibility hold area.

## Status
Contents here are **historical snapshots only** and may contain stale references to the old engine stack, including:
- `skills/video-production/core`
- legacy registry / DAG / WAL implementations

Do **not** treat these files as active runtime assets.

Authoritative archive target has moved to:
- `skills/.archived/`

Authoritative runtime has moved to:
- `core/infra/registry/`
- `core/engine/`

## Policy
- do not add new archived material here
- prefer reading from `skills/.archived/legacy-imports/` when available
- this directory may be removed in a later cleanup wave once all historical references are retired
